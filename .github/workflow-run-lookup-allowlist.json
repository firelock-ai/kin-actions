{
  "allowlist": [
    {
      "file": "scripts/recover-registry-publish.py",
      "reason": "Recovery acts on a run the caller already identified as failed. It re-drives that exact run rather than deciding anything from it, so there is no verification here to move onto immutable evidence.",
      "owner": "release-captain",
      "allow_match": [
        "repos/{repository}/actions/runs/{run_id}/"
      ]
    },
    {
      "file": "scripts/recover-release-pr-checks.py",
      "reason": "Recovery acts on a run the caller already identified as failed. It re-drives that exact run rather than deciding anything from it, so there is no verification here to move onto immutable evidence.",
      "owner": "release-captain",
      "allow_match": [
        "[\"api\", f\"repos/{repository}/actions/runs/{run_id}\"],",
        "f\"repos/{repository}/actions/runs/{run_id}/\""
      ]
    },
    {
      "file": "scripts/wait-tag-release-run.sh",
      "reason": "Reruns the tag-release run the mint just started, addressed by the id that start returned. The release verdict is still read from the tag and the attested release assets, never from this lookup.",
      "owner": "release-captain",
      "allow_match": [
        "gh run rerun \"$run_id\""
      ]
    },
    {
      "file": "scripts/check-workflow-pin-main.py",
      "reason": "The staged v0.1.34 pin rollout reads the exact landed main SHA's four release jobs immediately before advancing one consumer. This is bounded rollout admission, not durable release evidence, and must be replaced or removed before the exception expires.",
      "owner": "release-captain",
      "expiration": "2026-10-15",
      "allow_match": [
        "f\"repos/{repository}/actions/workflows/{workflow}/runs\",",
        "f\"repos/{repository}/actions/runs/{run_id}/jobs?per_page=100\","
      ]
    }
  ]
}
