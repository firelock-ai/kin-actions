# What a push to this branch proves. Edit these lines and push to run the same
# proof on other published bytes; a manual dispatch can name them instead.
KIN_VERSION=0.7.21
# 0.7.21 predates the kin setup client-failure fix, so its Windows contract run
# grades the old behaviour: the negative control.
SETUP_CONTRACT_EXPECT=old
